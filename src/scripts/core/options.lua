-- These are not dynamic. These are startup options. We're only storing them here
-- so we don't have to recompile the game when we change them. 
tdengine.options = {
  use_texture_atlas = 2,
  editor_font = 'inconsolata',
  editor_font_size = 16,
  dialogue_font = 'apple_kid',
  dialogue_font_size = 64
}
